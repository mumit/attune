# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
node_modules/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
env/

# Secrets and local config — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
token.json
client_secret*.json
service-account*.json

# Local runtime artifacts
audit.log.jsonl
setup-state.json
*.sqlite
*.db

# Eval-run output (build prompt 27) — generated by `attune eval run`/CI,
# never a checked-in fixture. evals/cases/, evals/triage_cases.json,
# evals/injection_corpus.json, and evals/agreement.json ARE tracked.
evals/reports/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Retry-queue SQLite (lives in ATTUNE_DATA_DIR in real deployments)
source_retries.db
source_retries.db-*

# Terraform local state, plans, and operator values (provider lock files commit)
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
*.auto.tfvars
backend.hcl
